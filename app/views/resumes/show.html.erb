<h1><%= @resume.title %>的履歷</h1>



<article data-controller="markdown-viewer"><%= @resume.content %></article>

<h3><%= if current_user && current_user.id === session[:thankyou9527] 
link_to "修改" , edit_resume_path end%></h3>